# AGENTS.md

Canonical agent instructions for this monorepo live in the private dashboard submodule:

**[apps/dashboard/AGENTS.md](apps/dashboard/AGENTS.md)**

Read and update that file only. This root file is a pointer so the public monorepo does not carry private workspace facts.

If `apps/dashboard` is not checked out, initialize it before monorepo-wide work (`git submodule update --init apps/dashboard`; needs org access / `REPO_CHECKOUT_PAT`).
