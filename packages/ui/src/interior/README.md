Vendored from [interior.dev](https://github.com/ddoemonn/interior) (MIT).

Import from `@lomi./ui/interior`, not the package root. These files target React 19 + `motion`; the shared primitives on `@lomi./ui` keep the existing Button/Dialog/Input APIs and use interior’s stone / 9px look.
