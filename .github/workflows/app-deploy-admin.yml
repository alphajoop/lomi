name: lomi · deploy · admin

on:
  repository_dispatch:
    types: [lomi-deploy-admin]
  push:
    branches: [main]
    paths:
      - "apps/admin/**"
      - ".github/workflows/app-deploy-admin.yml"
      - ".github/actions/checkout-private-app/**"
  workflow_dispatch:
    inputs:
      ref:
        description: Branch to deploy
        type: string
        default: main

concurrency:
  group: lomi-deploy-admin-${{ github.event.client_payload.pr_number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Load local actions
        uses: actions/checkout@v5
        with:
          sparse-checkout: .github/actions/checkout-private-app

      - name: Checkout app source
        id: checkout-app
        uses: ./.github/actions/checkout-private-app
        with:
          repository: lomiafrica/admin.lomi.africa
          submodule-path: apps/admin
          token: ${{ secrets.REPO_CHECKOUT_PAT }}
          event-name: ${{ github.event_name }}
          workflow-ref: ${{ inputs.ref }}
          dispatch-sha: ${{ github.event.client_payload.sha }}


      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v42.2.0
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID_ADMIN }}
          working-directory: ${{ github.workspace }}/${{ steps.checkout-app.outputs.app-path }}
          target: ${{ github.event.client_payload.target || 'production' }}
