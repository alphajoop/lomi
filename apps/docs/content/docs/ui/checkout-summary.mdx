---
title: Checkout summary card
description: Dark hosted-checkout product summary panel.
---

The `CheckoutSummaryCard` matches the dark left panel in hosted checkout.

## Preview

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview" className="p-0 mt-4 border-none bg-transparent">
    <ComponentPreview>
      <CheckoutSummaryCard
        title="Pay for Lomi Pro Plan"
        subtotal={4900}
        total={4900}
        currency="XOF"
      />
    </ComponentPreview>
  </Tab>

  <Tab value="Code" className="mt-4">
    ```tsx
    import { CheckoutSummaryCard } from '@/components/lomi-ui/checkout-summary-card';

    export default function Demo() {
      return (
        <CheckoutSummaryCard
          title="Pay for Lomi Pro Plan"
          subtotal={4900}
          total={4900}
          currency="XOF"
        />
      );
    }
    ```
  </Tab>
</Tabs>
