---
title: Payment Status Card
description: Hosted checkout success outcome card.
---

The `PaymentStatusCard` matches the success card in hosted checkout `post/success.tsx`.

## Preview

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview" className="p-0 mt-4 border-none bg-transparent">
    <ComponentPreview>
      <PaymentStatusCard
        organizationName="Keur Studio"
        primaryAction={{ label: 'Continue', href: '#' }}
      />
    </ComponentPreview>
  </Tab>

  <Tab value="Code" className="mt-4">
    ```tsx
    import { PaymentStatusCard } from '@/components/lomi-ui/payment-status-card';

    export default function Demo() {
      return (
        <PaymentStatusCard
          organizationName="Keur Studio"
          primaryAction={{ label: 'Continue', href: '/orders' }}
        />
      );
    }
    ```
  </Tab>
</Tabs>
