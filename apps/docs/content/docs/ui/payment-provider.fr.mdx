---
title: Sélecteur de fournisseur de paiement
description: Carrousel de moyens de paiement du checkout hébergé pour Wave, MTN, cartes et SPI.
---

Le `PaymentProviderSelector` correspond au carrousel du checkout hébergé dans `payment-provider-selection.tsx`.

## Aperçu

<Tabs items={['Aperçu', 'Code']}>
  <Tab value="Aperçu" className="p-0 mt-4 border-none bg-transparent">
    <ComponentPreview>
      <PaymentProviderSelectorDemo />
    </ComponentPreview>
  </Tab>

  <Tab value="Code" className="mt-4">
    ```tsx
    import { PaymentProviderSelector } from '@/components/lomi-ui/payment-provider-selector';

    export default function Demo() {
      return (
        <PaymentProviderSelector
          selectedProvider="WAVE"
          onProviderChange={(provider) => console.log(provider)}
        />
      );
    }
    ```
  </Tab>
</Tabs>
