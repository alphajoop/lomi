---
title: Checkout Mobile Money
description: Un formulaire de carte de checkout spécialisé pour les fournisseurs Mobile Money.
---

Le `MobileMoneyCheckoutCard` est un composant UI autonome, prêt à copier-coller, construit avec des éléments HTML standard et des classes utilitaires sémantiques Tailwind CSS. Il s'adapte automatiquement à votre thème sans nécessiter de primitives Shadcn supplémentaires.

## Aperçu

<Tabs items={['Aperçu', 'Code']}>
  <Tab value="Aperçu" className="p-0 mt-4 border-none bg-transparent">
    <ComponentPreview>
      <MobileMoneyCheckoutCard 
        amount={4900} 
        currency="XOF" 
        onSubmit={(phone) => alert(`Processing payment for ${phone}`)} 
      />
    </ComponentPreview>
  </Tab>

  <Tab value="Code" className="mt-4">
    ```tsx
    import { MobileMoneyCheckoutCard } from '@/components/lomi-ui/mobile-money-checkout-card';

    export default function Demo() {
      return (
        <MobileMoneyCheckoutCard 
          amount={4900} 
          currency="XOF" 
          onSubmit={(phone) => alert(`Processing payment for ${phone}`)} 
        />
      );
    }
    ```
  </Tab>
</Tabs>
