---
title: Carte de statut de paiement
description: Un composant pour communiquer le statut d'un paiement (en cours, réussi, échoué, etc.).
---

Le `PaymentStatusCard` est un composant UI autonome, prêt à copier-coller, construit avec des éléments HTML standard et des classes utilitaires sémantiques Tailwind CSS. Il s'adapte automatiquement à votre thème sans nécessiter de primitives Shadcn supplémentaires.

## Aperçu

<Tabs items={['Aperçu', 'Code']}>
  <Tab value="Aperçu" className="p-0 mt-4 border-none bg-transparent">
    <ComponentPreview>
      <PaymentStatusCard 
        status="processing" 
        amount={4900} 
        currency="XOF" 
      />
    </ComponentPreview>
  </Tab>

  <Tab value="Code" className="mt-4">
    ```tsx
    import { PaymentStatusCard } from '@/components/lomi-ui/payment-status-card';

    export default function Demo() {
      return (
        <PaymentStatusCard 
          status="processing" 
          amount={4900} 
          currency="XOF" 
        />
      );
    }
    ```
  </Tab>
</Tabs>
