---
title: Payment Provider Selector
description: Hosted-checkout payment method carousel for Wave, MTN, cards, and SPI.
---

The `PaymentProviderSelector` matches the hosted checkout carousel in `payment-provider-selection.tsx`.

## Preview

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview" className="p-0 mt-4 border-none bg-transparent">
    <ComponentPreview>
      <PaymentProviderSelector
        selectedProvider="WAVE"
        onProviderChange={() => undefined}
      />
    </ComponentPreview>
  </Tab>

  <Tab value="Code" className="mt-4">
    ```tsx
    import { PaymentProviderSelector } from '@/components/lomi-ui/payment-provider-selector';

    export default function Demo() {
      return (
        <PaymentProviderSelector
          selectedProvider="WAVE"
          onProviderChange={(provider) => console.log(provider)}
        />
      );
    }
    ```
  </Tab>
</Tabs>
