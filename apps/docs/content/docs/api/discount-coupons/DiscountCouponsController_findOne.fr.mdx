---
title: "Obtenir un coupon par ID"
description: "Obtenir un coupon par ID"
full: true
method: get
path: /discount-coupons/{id}
operationId: DiscountCouponsController_findOne
---

## Aperçu

Obtenir un coupon par ID

Renvoie un coupon. Réponse 404 s'il n'existe pas ou n'est pas accessible avec cette clé API.


### Quand l’utiliser

Utilisez cet endpoint quand vous devez exécuter `GET /discount-coupons/&#123;id&#125;` (Obtenir un coupon par ID).


## Authentification

Les routes marchandes nécessitent une clé API dans l’en-tête `X-API-KEY` (voir [Vue d’ensemble](/api)). Utilisez une clé **test** avec `https://sandbox.api.lomi.africa` et une clé **live** avec `https://api.lomi.africa`.

## Endpoint

`GET /discount-coupons/{id}`

URLs de base :

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Requête

### Paramètres d’URL

| Nom | Obligatoire | Schéma | Description |
| --- | --- | --- | --- |
| `id` | Oui | — | UUID du coupon |

### Paramètres de requête

_Aucun paramètre de requête._




## Réponses

| Statut | Description |
| --- | --- |
| `200` | Détails du coupon |
| `401` | Clé API invalide ou manquante |
| `404` | Coupon introuvable ou accès refusé |

## Erreurs

Les erreurs suivent un format JSON standard. **401** : clé manquante ou invalide. **404** : ressource introuvable. **429** : trop de requêtes. Pour les créations, envoyez une clé d’idempotence si votre flux le permet.

## Exemple

```bash
curl -sS -X GET "https://sandbox.api.lomi.africa/discount-coupons/value" \
  -H "X-API-KEY: $LOMI_SECRET_KEY"
```

## OpenAPI

- **operationId**: `DiscountCouponsController_findOne`
- **Operation**: `GET /discount-coupons/{id}`

Schémas complets et **Try it** : [référence API](/api).
