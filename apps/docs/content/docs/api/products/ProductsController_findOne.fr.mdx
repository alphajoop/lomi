---
title: "Obtenir un produit par ID"
description: "Obtenir un produit par ID"
full: true
method: get
path: /products/{id}
operationId: ProductsController_findOne
---

## Aperçu

Obtenir un produit par ID

Renvoie un produit avec ses prix et frais. Réponse 404 s'il n'existe pas ou n'est pas accessible avec cette clé API.


### Quand l’utiliser

Utilisez cet endpoint quand vous devez exécuter `GET /products/&#123;id&#125;` (Obtenir un produit par ID).


## Authentification

Les routes marchandes nécessitent une clé API dans l’en-tête `X-API-KEY` (voir [Vue d’ensemble](/api)). Utilisez une clé **test** avec `https://sandbox.api.lomi.africa` et une clé **live** avec `https://api.lomi.africa`.

## Endpoint

`GET /products/{id}`

URLs de base :

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Requête

### Paramètres d’URL

| Nom | Obligatoire | Schéma | Description |
| --- | --- | --- | --- |
| `id` | Oui | — | UUID du produit |

### Paramètres de requête

_Aucun paramètre de requête._




## Réponses

| Statut | Description |
| --- | --- |
| `200` | Détails du produit avec prix embarqués |
| `401` | Clé API invalide ou manquante |
| `404` | Produit introuvable ou accès refusé |

## Erreurs

Les erreurs suivent un format JSON standard. **401** : clé manquante ou invalide. **404** : ressource introuvable. **429** : trop de requêtes. Pour les créations, envoyez une clé d’idempotence si votre flux le permet.

## Exemple

```bash
curl -sS -X GET "https://sandbox.api.lomi.africa/products/value" \
  -H "X-API-KEY: $LOMI_API_KEY"
```

## OpenAPI

- **operationId**: `ProductsController_findOne`
- **Operation**: `GET /products/{id}`

Schémas complets et **Try it** : [référence API](/api).
