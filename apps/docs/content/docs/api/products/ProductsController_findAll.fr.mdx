---
title: "Lister les produits"
description: "Lister les produits"
full: true
method: get
path: /products
operationId: ProductsController_findAll
---

## Aperçu

Lister les produits

Renvoie tous les produits de l'organisation du marchand authentifié, avec prix et frais intégrés. Utile pour afficher le catalogue ou intégrer des systèmes externes.


### Quand l'utiliser

Utilisez cet endpoint pour `GET /products` (Lister les produits).


## Authentification

Les routes marchandes nécessitent une clé API dans l’en-tête `X-API-KEY` (voir [Vue d’ensemble](/api)). Utilisez une clé **test** avec `https://sandbox.api.lomi.africa` et une clé **live** avec `https://api.lomi.africa`.

## Endpoint

`GET /products`

URLs de base :

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Requête

### Paramètres de chemin

_Aucun paramètre de chemin au-delà du motif d’URL._


### Paramètres de requête

| Nom | Emplacement | Obligatoire | Schéma | Description |
| --- | --- | --- | --- | --- |
| `offset` | query | Non | — | Décalage pour la pagination |
| `limit` | query | Non | — | Nombre maximal de résultats |
| `isActive` | query | Non | — | Filtrer par statut actif |



## Réponses

| Statut | Description |
| --- | --- |
| `200` | Liste des produits avec prix embarqués |
| `401` | Clé API invalide ou manquante |

## Erreurs

Les erreurs suivent le format JSON standard (code de statut et message d'erreur). Validez les entrées avant appel ; **401** indique une clé manquante/invalide, **404** une ressource introuvable pour cette organisation, **429** une limite de requêtes. Pour les retries sûrs sur les créations, envoyez une clé d’idempotence quand votre flux le permet.

## Exemple

```bash
curl -sS -X GET "https://sandbox.api.lomi.africa/products?offset=1&limit=1" \
  -H "X-API-KEY: $LOMI_API_KEY"
```

## OpenAPI

- **operationId**: `ProductsController_findAll`
- **Operation**: `GET /products`

Schémas complets et **Try it** : [référence API](/api).
