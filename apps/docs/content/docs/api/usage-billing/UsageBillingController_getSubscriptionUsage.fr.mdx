---
title: "Get meter usage for a subscription"
description: "Get meter usage for a subscription"
full: true
method: get
path: /usage-billing/subscriptions/{subscriptionId}/usage
operationId: UsageBillingController_getSubscriptionUsage
---

## Aperçu

Get meter usage for a subscription

### Quand l’utiliser

Utilisez cet endpoint quand vous devez exécuter `GET /usage-billing/subscriptions/&#123;subscriptionId&#125;/usage` (Get meter usage for a subscription).


## Authentification

Les routes marchandes nécessitent une clé API dans l’en-tête `X-API-KEY` (voir [Vue d’ensemble](/api)). Utilisez une clé **test** avec `https://sandbox.api.lomi.africa` et une clé **live** avec `https://api.lomi.africa`.

## Endpoint

`GET /usage-billing/subscriptions/{subscriptionId}/usage`

URLs de base :

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Requête

### Paramètres d’URL

_Pas d’autre paramètre dans l’URL._


### Paramètres de requête

_Aucun paramètre de requête._




## Réponses

| Statut | Description |
| --- | --- |
| `200` | - |

## Erreurs

Les erreurs suivent un format JSON standard. **401** : clé manquante ou invalide. **404** : ressource introuvable. **429** : trop de requêtes. Pour les créations, envoyez une clé d’idempotence si votre flux le permet.

## Exemple

```bash
curl -sS -X GET "https://sandbox.api.lomi.africa/usage-billing/subscriptions/subscriptionId_value/usage" \
  -H "X-API-KEY: $LOMI_SECRET_KEY"
```

## OpenAPI

- **operationId**: `UsageBillingController_getSubscriptionUsage`
- **Operation**: `GET /usage-billing/subscriptions/{subscriptionId}/usage`

Schémas complets et **Try it** : [référence API](/api).
