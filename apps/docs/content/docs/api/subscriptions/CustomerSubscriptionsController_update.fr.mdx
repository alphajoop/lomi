---
title: "Update customer subscription"
description: "Update customer subscription"
full: true
method: patch
path: /customer-subscriptions/{subscription_id}
operationId: CustomerSubscriptionsController_update
---

## Aperçu

Update customer subscription

### Quand l’utiliser

Utilisez cet endpoint quand vous devez exécuter `PATCH /customer-subscriptions/&#123;subscription_id&#125;` (Update customer subscription).


## Authentification

Les routes marchandes nécessitent une clé API dans l’en-tête `X-API-KEY` (voir [Vue d’ensemble](/api)). Utilisez une clé **test** avec `https://sandbox.api.lomi.africa` et une clé **live** avec `https://api.lomi.africa`.

## Endpoint

`PATCH /customer-subscriptions/{subscription_id}`

URLs de base :

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Requête

### Paramètres d’URL

_Pas d’autre paramètre dans l’URL._


### Paramètres de requête

_Aucun paramètre de requête._


### Corps de la requête

_Pas de corps JSON pour cette opération._


## Réponses

| Statut | Description |
| --- | --- |
| `200` | - |

## Erreurs

Les erreurs suivent un format JSON standard. **401** : clé manquante ou invalide. **404** : ressource introuvable. **429** : trop de requêtes. Pour les créations, envoyez une clé d’idempotence si votre flux le permet.

## Exemple

```bash
curl -sS -X PATCH "https://sandbox.api.lomi.africa/customer-subscriptions/subscription_id_value" \
  -H "X-API-KEY: $LOMI_SECRET_KEY" \
  -H "Content-Type: application/json" \
  -d '{}'
```

## OpenAPI

- **operationId**: `CustomerSubscriptionsController_update`
- **Operation**: `PATCH /customer-subscriptions/{subscription_id}`

Schémas complets et **Try it** : [référence API](/api).
