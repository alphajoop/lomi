---
title: "List customer subscriptions"
description: "List customer subscriptions"
full: true
method: get
path: /customer-subscriptions
operationId: CustomerSubscriptionsController_findAll
---

## Aperçu

List customer subscriptions

### Quand l’utiliser

Utilisez cet endpoint quand vous devez exécuter `GET /customer-subscriptions` (List customer subscriptions).


## Authentification

Les routes marchandes nécessitent une clé API dans l’en-tête `X-API-KEY` (voir [Vue d’ensemble](/api)). Utilisez une clé **test** avec `https://sandbox.api.lomi.africa` et une clé **live** avec `https://api.lomi.africa`.

## Endpoint

`GET /customer-subscriptions`

URLs de base :

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Requête

### Paramètres d’URL

_Pas d’autre paramètre dans l’URL._


### Paramètres de requête

| Nom | Emplacement | Obligatoire | Schéma | Description |
| --- | --- | --- | --- | --- |
| `offset` | query | Non | - |  |
| `limit` | query | Non | - |  |
| `status` | query | Non | - |  |
| `customer_id` | query | Non | - |  |



## Réponses

| Statut | Description |
| --- | --- |
| `200` | - |

## Erreurs

Les erreurs suivent un format JSON standard. **401** : clé manquante ou invalide. **404** : ressource introuvable. **429** : trop de requêtes. Pour les créations, envoyez une clé d’idempotence si votre flux le permet.

## Exemple

```bash
curl -sS -X GET "https://sandbox.api.lomi.africa/customer-subscriptions?offset=1&limit=1" \
  -H "X-API-KEY: $LOMI_SECRET_KEY"
```

## OpenAPI

- **operationId**: `CustomerSubscriptionsController_findAll`
- **Operation**: `GET /customer-subscriptions`

Schémas complets et **Try it** : [référence API](/api).
