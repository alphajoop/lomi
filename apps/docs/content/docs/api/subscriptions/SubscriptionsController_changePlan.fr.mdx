---
title: "Changer le plan tarifaire"
description: "Met à jour le tarif d’un abonnement actif"
full: true
method: post
path: /subscriptions/{id}/change-plan
operationId: SubscriptionsController_changePlan
---

## Aperçu

Met à jour le `price_id` d’un abonnement actif (montée ou descente de gamme).

### Quand l'utiliser

Utilisez cet endpoint lorsqu’un client passe à un autre tarif récurrent.

### Voir aussi

[Mettre à jour un abonnement](/api/subscriptions/SubscriptionsController_update)

## Authentification

Les routes marchandes nécessitent une clé API dans l’en-tête `X-API-KEY` (voir [Vue d’ensemble](/api)). Utilisez une clé **test** avec `https://sandbox.api.lomi.africa` et une clé **live** avec `https://api.lomi.africa`.

## Endpoint

`POST /subscriptions/{id}/change-plan`

URLs de base :

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Requête

### Paramètres de chemin

| Nom | Obligatoire | Schéma | Description |
| --- | --- | --- | --- |
| `id` | Oui | `string` (uuid) | UUID de l’abonnement |

### Corps de requête

Corps JSON.

| Champ | Obligatoire | Type | Description |
| --- | --- | --- | --- |
| `price_id` | Oui | `string` (uuid) | UUID du tarif cible |

Exemple :

```json
{
  "price_id": "3fa85f64-5717-4562-b3fc-2c963f66afa6"
}
```

## Réponses

| Statut | Description |
| --- | --- |
| `200` | Abonnement mis à jour |
| `401` | Clé API invalide ou manquante |
| `404` | Abonnement introuvable ou accès refusé |

## Erreurs

Les erreurs suivent le format JSON standard (code HTTP et message d'erreur). Validez les entrées avant l’appel ; **401** indique une clé manquante ou invalide, **404** une ressource introuvable pour cette organisation, **429** une limite de requêtes.

## Exemple

```bash
curl -sS -X POST "https://sandbox.api.lomi.africa/subscriptions/{id}/change-plan" \
  -H "X-API-KEY: $LOMI_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"price_id":"3fa85f64-5717-4562-b3fc-2c963f66afa6"}'
```

## OpenAPI

- **operationId** : `SubscriptionsController_changePlan`
- **Opération** : `POST /subscriptions/{id}/change-plan`

Schémas complets et **Essayer** : [Référence API](/api/index). Contrat machine : `apps/docs/openapi.json` dans le dépôt.
