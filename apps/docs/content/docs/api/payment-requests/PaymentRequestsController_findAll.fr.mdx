---
title: "Lister les demandes de paiement"
description: "Lister les demandes de paiement"
full: true
method: get
path: /payment-requests
operationId: PaymentRequestsController_findAll
---

## Aperçu

Lister les demandes de paiement

Renvoie toutes les demandes de l'organisation avec pagination et filtres optionnels (statut, client).


### Quand l’utiliser

Utilisez cet endpoint quand vous devez exécuter `GET /payment-requests` (Lister les demandes de paiement).


## Authentification

Les routes marchandes nécessitent une clé API dans l’en-tête `X-API-KEY` (voir [Vue d’ensemble](/api)). Utilisez une clé **test** avec `https://sandbox.api.lomi.africa` et une clé **live** avec `https://api.lomi.africa`.

## Endpoint

`GET /payment-requests`

URLs de base :

- `https://sandbox.api.lomi.africa`
- `https://api.lomi.africa`

## Requête

### Paramètres d’URL

_Pas d’autre paramètre dans l’URL._


### Paramètres de requête

| Nom | Emplacement | Obligatoire | Schéma | Description |
| --- | --- | --- | --- | --- |
| `offset` | query | Non | — | Décalage pour la pagination |
| `limit` | query | Non | — | Nombre maximal de résultats |
| `customerId` | query | Non | — | Filtrer par ID client |
| `status` | query | Non | — | Filtrer par statut |



## Réponses

| Statut | Description |
| --- | --- |
| `200` | Liste paginée de demandes |
| `401` | Clé API invalide ou manquante |

## Erreurs

Les erreurs suivent un format JSON standard. **401** : clé manquante ou invalide. **404** : ressource introuvable. **429** : trop de requêtes. Pour les créations, envoyez une clé d’idempotence si votre flux le permet.

## Exemple

```bash
curl -sS -X GET "https://sandbox.api.lomi.africa/payment-requests?offset=1&limit=1" \
  -H "X-API-KEY: $LOMI_SECRET_KEY"
```

## OpenAPI

- **operationId**: `PaymentRequestsController_findAll`
- **Operation**: `GET /payment-requests`

Schémas complets et **Try it** : [référence API](/api).
