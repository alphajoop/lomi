---
title: Démarrage rapide
description: Installez, personnalisez et utilisez les composants checkout lomi. UI.
---

import {
  CheckoutFooterDemo,
  CheckoutSubmitButtonDemo,
  CheckoutSummaryCardDemo,
  CustomerInformationSectionDemo,
  MobileMoneyCheckoutCardDemo,
  PaymentFailureCardDemo,
  PaymentProviderSelectorDemo,
  PaymentStatusCardDemo,
  PriceSelectorDemo,
  PricingTableDemo,
  SubscriptionManagementCardDemo,
  UsageMeterDemo,
} from '@/components/lomi-ui/demo';

# Démarrage rapide

Installez un composant avec le CLI shadcn :

```bash
npx shadcn@latest add https://docs.lomi.africa/r/mobile-money-checkout-card.json
```

Ou avec le CLI lomi. :

```bash
lomi ui add payment-provider-selector
```

Lister tous les composants : `lomi ui list`. Mettre à jour les copies installées : `lomi ui update`. Voir [Commandes CLI UI](/build/cli/ui).

Les composants n'appellent pas l'API lomi. directement. Utilisez votre backend ou l'intégration SDK pour créer des sessions de checkout, des demandes de paiement et la gestion des webhooks.

## Chemins des assets

Copiez les assets du checkout hébergé dans le dossier public de votre application :

- `/payment_channels/wave.webp`
- `/payment_channels/mtn.webp`
- `/payment_channels/pi_spi.webp`
- `/placeholder/card.webp`
- `/placeholder/bnpl.webp`

## Dépendances

Les composants de formulaire client nécessitent :

```bash
npm install react-phone-number-input lucide-react
```

## Mise à jour depuis d'anciennes copies lomi. UI

Si vous avez installé des composants avant la synchronisation avec le checkout hébergé, relancez `lomi ui update` et adaptez les identifiants de fournisseur :

| Avant | Après |
| ----- | ----- |
| `wave` | `WAVE` |
| `mtn` | `MTN` |
| `card` | `cards` |
| `spi` | `spi` (inchangé) |

Le BNPL utilise `bnpl`.

## Thème

Les composants de paiement lomi. UI reprennent le style du checkout hébergé : bleu de sélection codé en dur, bordures grises subtiles, panneaux de paiement blancs et pas d'anneaux de focus sur les champs checkout.

### Tokens du checkout hébergé

| Token | Valeur production |
| ----- | ----------------- |
| Bordure / badge sélectionné | `#56A5F9` |
| Fond sélectionné (rendu) | Blanc — le `checkout.css` de production remplace `bg-slate-100/50` sur `.border.bg-card` |
| Bordure non sélectionnée | `border-border/40` |
| Fond par défaut du bouton payer | `#121317` |
| Rayon de carte | `rounded-sm` |

Comme chaque composant est copié dans votre application, personnalisez les classes directement. Gardez l'état sélectionné `#56A5F9` évident sur les cartes fournisseur.

## Composants

### Sélecteur de fournisseur de paiement

<PaymentProviderSelectorDemo />

<Installation name="payment-provider-selector" />

Identifiants : `WAVE`, `MTN`, `cards`, `spi`, `bnpl`.

### Section informations client

<CustomerInformationSectionDemo />

<Installation name="customer-information-section" />

Nécessite `react-phone-number-input` dans votre application.

### Bouton de soumission checkout

<CheckoutSubmitButtonDemo />

<Installation name="checkout-submit-button" />

### Carte checkout mobile money

<MobileMoneyCheckoutCardDemo />

<Installation name="mobile-money-checkout-card" />

### Carte récapitulatif checkout

<CheckoutSummaryCardDemo />

<Installation name="checkout-summary-card" />

### Sélecteur de prix

<PriceSelectorDemo />

<Installation name="price-selector" />

Affichez-le dans un panneau `#121317` pour correspondre au checkout hébergé.

### Carte statut de paiement

<PaymentStatusCardDemo />

<Installation name="payment-status-card" />

### Carte échec de paiement

<PaymentFailureCardDemo />

<Installation name="payment-failure-card" />

### Pied de page checkout

<CheckoutFooterDemo />

<Installation name="checkout-footer" />

### Tableau de tarification

Composant marketing autonome. Pour le cycle de facturation du checkout hébergé, utilisez le [Sélecteur de prix](#sélecteur-de-prix).

<PricingTableDemo />

<Installation name="pricing-table" />

### Carte gestion d'abonnement

<SubscriptionManagementCardDemo />

<Installation name="subscription-management-card" />

### Compteur d'utilisation

<UsageMeterDemo />

<Installation name="usage-meter" />
